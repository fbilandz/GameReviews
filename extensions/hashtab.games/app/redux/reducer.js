import { combineReducers } from 'redux';
import _ from 'lodash';

const reviews = (state = {}, action) => {
  switch(action.type){
    case 1:
    // REVIEWS_LOADING, REVIEWS_LOADED, REVIEWS_ERROR
    default:
      return state;
  }
}

export default combineReducers({

});